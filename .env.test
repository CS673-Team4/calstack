# Test environment configuration
# Copy this to .env.test and modify as needed for testing

# Application URL for testing
TEST_BASE_URL=http://localhost:5000

# Test user credentials (for manual testing)
TEST_EMAIL=test@example.com
TEST_PASSWORD=testpassword123

# MongoDB settings for testing (if using separate test DB)
# MONGO_URI=mongodb://localhost:27017/calstack_test

# Disable external services during testing
SENDGRID_API_KEY=test_key_disabled
FLASK_DEBUG=1
FLASK_SECRET_KEY=test_secret_key_for_testing_only

# OAuth settings (use test credentials if available)
# =test_client_id
# GOOGLE_CLIENT_SECRET=test_client_secret
# MS_CLIENT_ID=test_ms_client_id
# MS_CLIENT_SECRET=test_ms_client_secret
